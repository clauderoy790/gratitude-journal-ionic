import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roy.dailygratitude',
  appName: 'Daily Gratitude',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
